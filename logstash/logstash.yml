input {
  beats {
    port => 5044
  }   
}   

filter {
  if [tags][json] {
    json {
      source => "message"
    }   
  }   
}   

output {
  stdout { codec => rubydebug { metadata => true } } 
  elasticsearch { 
    hosts => ["elasticsearch:9200"] 
  }
}

setup.kibana:
  host: "kibana:5601"